[package]
name = "airspring-exp001-local-science-parity"
version = "0.1.0"
edition = "2024"
publish = false
license = "AGPL-3.0-or-later"
description = "Validates airSpring science methods produce identical results via direct Rust call vs JSON-RPC dispatch (in-process)"

[workspace]

[dependencies]
airspring-barracuda = { path = "../../barracuda" }
serde_json = "1"
